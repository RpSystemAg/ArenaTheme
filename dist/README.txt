This package bundles both installable components. WordPress installs a theme and a plugin from separate packages, so for one-click installs use the individual archives.

Contents:
  arena-commerce.zip  -> install via Appearance > Themes > Add New > Upload Theme
  arena-engine.zip    -> install via Plugins > Add New > Upload Plugin
